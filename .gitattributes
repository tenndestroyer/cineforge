# Keep Windows scripts CRLF (cmd.exe/PowerShell are happiest with CRLF) and
# shell scripts LF, regardless of the committer's platform.
*.bat   text eol=crlf
*.ps1   text eol=crlf
*.cmd   text eol=crlf
*.sh    text eol=lf
* text=auto
